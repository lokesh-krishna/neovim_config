{
  "libraries": {
    "prefix": [
      "libraries"
    ],
    "description": "insert commonly used libraries",
    "body": [
      "# Libraries",
      "library(tidyverse)",
      "library(here)",
      "library(janitor)"
    ]
  },
  "function": {
    "prefix": [
      "function"
    ],
    "description": "insert function template",
    "body": [
      "${1:name} <- function(${2:variable}) {",
      "  ${0}",
      "}"
    ]
  }
}
